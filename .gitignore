# Dependencies
node_modules/

# Build output
dist/
build/

# Test coverage
coverage/

# Environment files (keep .env.example)
.env
.env.*
!.env.example

# Local deployment state — testnet ids regenerated by the issuer scripts (ADR-0007).
# Keep the committed example that documents the shape.
deployments.json

# Logs
*.log
npm-debug.log*

# OS files
.DS_Store

# Editor
.vscode/*
!.vscode/extensions.json

# Vite (frontend, later cycles)
.vite/
frontend/dist/

# Hardhat (optional hybrid module, later cycle) — scoped to contracts/
contracts/cache/
contracts/artifacts/
contracts/typechain-types/
